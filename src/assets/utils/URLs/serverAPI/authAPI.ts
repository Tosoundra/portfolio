import { SERVER_API } from './serverAPI';

export const SERVER_SIGN_UP_URL = `${SERVER_API}/sign-up`;
export const SERVER_SIGN_IN_URL = `${SERVER_API}/sign-in`;
export const SERVER_SIGN_OUT_URL = `${SERVER_API}/sign-out`;
